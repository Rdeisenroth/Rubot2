export enum QueueEvent {
    JOIN = 'join',
    LEAVE = 'leave',
    KICK = 'kick',
    NEXT = 'next',
    TUTOR_SESSION_START = 'tutor_session_start',
    TUTOR_SESSION_QUIT = 'tutor_session_quit',
}
